import 'package:flutter/material.dart';

class CanSwipeChart extends StatelessWidget {
  const CanSwipeChart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
